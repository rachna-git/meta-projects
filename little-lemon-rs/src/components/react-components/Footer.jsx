const Footer = () => {
  return (
    <>
      <footer>Footer</footer>
    </>
  );
};

export default Footer;
// This is a functional component that renders a footer element with the text "Footer".
// It is exported as the default export of the module.
